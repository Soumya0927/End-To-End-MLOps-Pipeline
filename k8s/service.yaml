apiVersion: v1
kind: Service
metadata:
  name: fraud-detection-service
  namespace: oppe2-app
spec:
  type: LoadBalancer
  selector:
    app: fraud-detection-api
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8200

